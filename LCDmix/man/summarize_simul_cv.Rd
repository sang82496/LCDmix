% Generated by roxygen2: do not edit by hand
% Please edit documentation in create-LCDmix.Rmd.
\name{summarize_simul_cv}
\alias{summarize_simul_cv}
\title{Summarize CV results across simulations and pick best lambdas}
\usage{
summarize_simul_cv(simul_idx_mat, save_dir)
}
\arguments{
\item{simul_idx_mat}{Numeric matrix of CV jobs (e.g., from
\code{simul_idx_matrix()}). Must contain a \code{sim_idx} column; all other
columns required by your \code{cv_lcd_summary()} are passed along after removing
\code{sim_idx}.}

\item{save_dir}{Character path to the directory containing the per–CV-run
result files produced by \code{cv_lcd_simul()}.}
}
\value{
A numeric matrix with three columns:
\describe{
\item{\code{sim_idx}}{Simulation identifier.}
\item{\code{lambda_alpha}}{Selected \code{lambda_alpha} for that simulation.}
\item{\code{lambda_theta}}{Selected \code{lambda_theta} for that simulation.}
}
}
\description{
For each unique simulation ID in \code{simul_idx_mat}, this helper subsets the
corresponding rows, calls \code{cv_lcd_summary()} to aggregate the CV results for
that simulation, and extracts the optimal penalty pair
\code{(lambda_alpha, lambda_theta)}. It returns a compact matrix with one row
per simulation.
}
\examples{
\dontrun{
# Suppose you already ran:
# logs <- cv_lcd_simul(idx_mat, sim_dir="sim_data", K=2, save_dir="cv_saves")

best_tbl <- summarize_simul_cv(
  simul_idx_mat = idx_mat,
  save_dir      = "cv_saves"
)
head(best_tbl)
}

}