% Generated by roxygen2: do not edit by hand
% Please edit documentation in create-LCDmix.Rmd.
\name{run_cv_simul}
\alias{run_cv_simul}
\title{Run cross-validation jobs across many simulations in parallel}
\usage{
run_cv_simul(
  simul_idx_mat,
  sim_dir = "sim_data",
  K,
  max_iter = 30,
  iter_eta = 0.001,
  resp_threshold = 0.001,
  nfold = 5,
  trim_prob = 0.01,
  save_dir = "cv_saves",
  n_cores = "max",
  blocksize = 5,
  maxdev = NULL,
  n_restarts = 1
)
}
\arguments{
\item{simul_idx_mat}{Numeric matrix where each row is one job. Must contain
columns \code{sim_idx}, \code{alpha_idx}, \code{theta_idx}, \code{seed_idx},
\code{fold_idx}, and the corresponding \code{lambda_alpha}, \code{lambda_theta}
values (e.g., as produced by \code{simul_idx_matrix()}).}

\item{sim_dir}{Character path to the directory with simulation files named
\code{sim_<sim_idx>.rds}. Default: \code{"sim_data"}.}

\item{K}{Integer number of mixture components.}

\item{max_iter}{Integer; maximum EM iterations per fit. Default: 30.}

\item{iter_eta}{Numeric; convergence threshold on relative change in Q. Default: 1e-3.}

\item{resp_threshold}{Numeric in \link{0,1}; responsibilities below this are zeroed
for numerical stability. Default: 1e-3.}

\item{nfold}{Integer number of CV folds (used when rebuilding folds for each simulation).
Default: 5.}

\item{trim_prob}{Numeric in \link{0,0.5}; fraction of lowest-likelihood points to trim
when averaging held-out log-likelihood. Default: 0.01.}

\item{save_dir}{Character path to write per-job results (\code{*.Rdata}).
Files are saved as \code{<sim_idx>-<alpha_idx>-<theta_idx>-<seed_idx>-<fold_idx>.Rdata}.
Default: \code{"cv_saves"}.}

\item{n_cores}{Integer or \code{"max"}; number of parallel workers. \code{"max"}
uses all physical cores minus one. Default: \code{"max"}.}

\item{blocksize}{Integer block size passed to \code{flowmix::make_cv_folds()}.
Default: 5.}

\item{maxdev}{Numeric or \code{NULL}; max-deviation constraint for LP updates
in theta-step. Default: \code{NULL}.}

\item{n_restarts}{Integer; number of random restarts for initialization in \code{main()}.
Default: 1.}
}
\value{
A character vector of per-job log messages, followed by a summary line
reporting the number and fraction of failures (jobs where fitting or evaluation failed).
}
\description{
Executes one CV job per row of \code{simul_idx_mat} in parallel. Each row
identifies a simulation file, a fold, a random seed, and a pair of
penalty indices/values. For each job, the function:
(1) loads the simulation from \code{sim_dir}, (2) rebuilds CV folds,
(3) fits the LCD mixture model on the train split with \code{main()},
(4) evaluates on the held-out split via \code{evaluate_lcd_model()},
and (5) saves \code{prop_CV}, \code{trimmed_CV}, and a text log to \code{save_dir}.
}
\examples{
\dontrun{
# Build an index matrix over sims × folds × seeds × penalties
idx <- simul_idx_matrix(
  n_sims        = 2,
  alpha_lambdas = c(1e-3, 1e-2),
  theta_lambdas = c(1e-3, 1e-2),
  seeds         = 1:2,
  nfold         = 3
)
# Run CV jobs and save results under "cv_saves/"
logs <- run_cv_simul(
  simul_idx_mat = idx,
  sim_dir       = "sim_data",
  K             = 2,
  save_dir      = "cv_saves",
  n_cores       = "max"
)
cat(tail(logs), sep = "\n")
}

}