% Generated by roxygen2: do not edit by hand
% Please edit documentation in create-LCDmix.Rmd.
\name{cv_lcd_parallel}
\alias{cv_lcd_parallel}
\title{Cross‐validate the LCD mixture‐of‐experts model in parallel}
\usage{
cv_lcd_parallel(
  Y_bin,
  X,
  bin_mass,
  K,
  lambda_alpha_range = c(1e-08, 0.1),
  lambda_theta_range = c(1e-08, 0.1),
  cv_grid_size = 5,
  n_restarts = 1,
  max_iter = 30,
  iter_eta = 0.001,
  maxdev = NULL,
  resp_threshold = 0.001,
  n_folds = 5,
  block_size = 5,
  cv_reps = 5,
  trim_prob = 0.05,
  save_dir = "./result",
  sparseMatrix = TRUE,
  n_cores = "max"
)
}
\arguments{
\item{Y_bin}{List of length \eqn{TT}, where each element is an \eqn{M_t \times 1}
matrix of binned responses per time point.}

\item{X}{Numeric \eqn{TT \times p} matrix of covariates (rows = time points).}

\item{bin_mass}{List of length \eqn{TT}, where each element is a numeric vector of
biomass weights per bin at time \eqn{t}.}

\item{K}{Integer number of mixture components.}

\item{lambda_alpha_range}{Numeric vector of length 2 giving the min and max values
for the \code{lambda_alpha} grid (on a log scale). Default: \code{c(1e-8, 1e-1)}.}

\item{lambda_theta_range}{Numeric vector of length 2 giving the min and max values
for the \code{lambda_theta} grid (on a log scale). Default: \code{c(1e-8, 1e-1)}.}

\item{cv_grid_size}{Integer number of candidate lambdas per penalty. Default: 5.}

\item{n_restarts}{Integer number of random restarts for \code{flowmix::flowmix()}. Default: 1.}

\item{max_iter}{Integer maximum number of EM iterations per model fit. Default: 30.}

\item{iter_eta}{Numeric convergence threshold (relative change in Q). Default: 1e-3.}

\item{maxdev}{Numeric or \code{NULL}; max‐deviation constraint for LP updates. Default: \code{NULL}.}

\item{resp_threshold}{Numeric in [0,1]; responsibilities below this are zeroed. Default: 1e-3.}

\item{n_folds}{Integer number of cross‐validation folds. Default: 5.}

\item{block_size}{Integer block size for folding. Default: 5.}

\item{cv_reps}{Integer number of repeated CV runs. Default: 5.}

\item{trim_prob}{Numeric in [0,1]; fraction of lowest‐likelihood points to trim when averaging. Default: 0.05.}

\item{save_dir}{Character; directory in which to save per‐run results. Default: "./result".}

\item{sparseMatrix}{Logical; if \code{TRUE}, build LP constraints as sparse matrices. Default: \code{TRUE}.}

\item{n_cores}{Integer or "max"; number of parallel worker processes. "max" uses all physical cores minus one. Default: "max".}
}
\value{
A list with components:
\describe{
\item{logs}{Character vector of log messages (one per CV run, plus a summary).}
\item{index_matrix}{Numeric matrix with one row per combination (fold × rep × penalties),
showing indices and actual \code{lambda_alpha}, \code{lambda_theta} values.}
}
}
\description{
Performs grid‐search cross‐validation over the L1 penalties \code{lambda_alpha}
and \code{lambda_theta} for the log‐concave mixture‐of‐experts model.  Creates
folds (with optional blocking), runs multiple repetitions per fold, and evaluates
models on held‐out data.  Computations are dispatched in parallel.
}
\examples{
\dontrun{
# 2‐component model, small grid, 3 folds, 2 reps
results <- cv_lcd_parallel(
  Y_bin               = Y_binned,
  X                   = X_mat,
  bin_mass            = bin_mass_list,
  K                   = 2,
  lambda_alpha_range  = c(1e-4, 1e-2),
  lambda_theta_range  = c(1e-4, 1e-2),
  cv_grid_size        = 4,
  n_folds             = 3,
  cv_reps             = 2,
  n_cores             = "max"
)
}
}