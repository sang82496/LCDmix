% Generated by roxygen2: do not edit by hand
% Please edit documentation in create-LCDmix.Rmd.
\name{cv_lcd_parallel}
\alias{cv_lcd_parallel}
\title{Cross‐validate the LCD mixture‐of‐experts model in parallel}
\usage{
cv_lcd_parallel(
  Y_bin,
  X,
  bin_mass,
  K,
  lambda_alpha_range = c(1e-08, 0.1),
  lambda_theta_range = c(1e-08, 0.1),
  cv_gridsize = 5,
  max_iter = 30,
  iter_eta = 0.001,
  resp_threshold = 0.001,
  nfold = 5,
  seeds = NULL,
  trim_prob = 0.05,
  save_dir = "./result",
  n_cores = "max",
  cv_reps = NULL,
  blocksize = 5,
  maxdev = NULL,
  n_restarts = 1
)
}
\arguments{
\item{Y_bin}{List of length \eqn{TT}, where each element is an \eqn{M_t \times 1}
matrix of binned responses per time point.}

\item{X}{Numeric \eqn{TT \times p} matrix of covariates (rows = time points).}

\item{bin_mass}{List of length \eqn{TT}, where each element is a numeric vector of
biomass weights per bin at time \eqn{t}.}

\item{K}{Integer number of mixture components.}

\item{lambda_alpha_range}{Numeric vector of length 2 giving the min and max values
for the \code{lambda_alpha} grid (on a log scale). Default: \code{c(1e-8, 1e-1)}.}

\item{lambda_theta_range}{Numeric vector of length 2 giving the min and max values
for the \code{lambda_theta} grid (on a log scale). Default: \code{c(1e-8, 1e-1)}.}

\item{cv_gridsize}{Integer number of candidate lambdas per penalty. Default: 5.}

\item{max_iter}{Integer maximum number of EM iterations per model fit. Default: 30.}

\item{iter_eta}{Numeric convergence threshold (relative change in Q). Default: 1e-3.}

\item{resp_threshold}{Numeric in [0,1]; responsibilities below this are zeroed. Default: 1e-3.}

\item{nfold}{Integer number of cross‐validation folds. Default: 5.}

\item{seeds}{Integer vector of random seeds (one per repeat).  Must supply either
\code{seeds} or \code{cv_reps}, not both. Default: \code{NULL}.}

\item{trim_prob}{Numeric fraction of lowest‐likelihood observations to trim when averaging. Default: 0.05.}

\item{save_dir}{Character; directory in which to save per‐run results. Default: "./result".}

\item{n_cores}{Integer or "max"; number of parallel worker processes. "max" uses all physical cores minus one. Default: "max".}

\item{cv_reps}{Integer number of repeated CV runs (used when \code{seeds} is \code{NULL}). Default: \code{NULL}.}

\item{blocksize}{Integer block size for CV fold creation. Default: 5.}

\item{maxdev}{Numeric or \code{NULL}; maximum deviation for LP constraints. Default: \code{NULL}.}

\item{n_restarts}{Integer number of random restarts for the initial GMR. Default: 1.}
}
\value{
A list with components:
\describe{
\item{logs}{Character vector of log messages (one per CV run, plus summary).}
\item{index_matrix}{Integer matrix with one row per run (fold × seed × penalties),
showing \code{alpha_idx}, \code{theta_idx}, \code{seed_idx}, \code{fold_idx},
and actual \code{lambda_alpha}, \code{lambda_theta} values.}
}
}
\description{
Performs grid‐search cross‐validation over the L1 penalties \code{lambda_alpha}
and \code{lambda_theta} for the log‐concave mixture‐of‐experts model.  Creates
folds (with optional blocking), repeats each fold either by explicit seeds
or by a count of repeats, and evaluates models on held‐out data.  Computations
are dispatched in parallel, and per‐run results are saved.
}
\examples{
\dontrun{
# Four folds, three repeats via explicit seeds
seeds <- c(101, 202, 303)
results <- cv_lcd_parallel(
  Y_bin               = Y_binned,
  X                   = X_mat,
  bin_mass            = counts_list,
  K                   = 2,
  cv_gridsize         = 4,
  nfold               = 4,
  seeds               = seeds,
  n_cores             = "max"
)

# Or specify number of repeats instead of seeds
results2 <- cv_lcd_parallel(
  Y_bin               = Y_binned,
  X                   = X_mat,
  bin_mass            = counts_list,
  K                   = 2,
  cv_gridsize         = 4,
  nfold               = 4,
  n_cores             = 2,
  cv_reps             = 3
)
}
}