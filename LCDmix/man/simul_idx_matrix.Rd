% Generated by roxygen2: do not edit by hand
% Please edit documentation in create-LCDmix.Rmd.
\name{simul_idx_matrix}
\alias{simul_idx_matrix}
\title{Build a CV job index matrix across simulations, folds, seeds, and penalties}
\usage{
simul_idx_matrix(n_sims, alpha_lambdas, theta_lambdas, seeds, nfold)
}
\arguments{
\item{n_sims}{Integer number of simulated datasets (files \code{sim_<i>.rds}).}

\item{alpha_lambdas}{Numeric vector of candidate \code{lambda_alpha} values.}

\item{theta_lambdas}{Numeric vector of candidate \code{lambda_theta} values.}

\item{seeds}{Vector of random seeds to use for repeated CV runs.}

\item{nfold}{Integer number of CV folds per simulation.}
}
\value{
A numeric matrix with columns:
\describe{
\item{sim_idx}{Simulation index (1..\code{n_sims}).}
\item{alpha_idx}{Index into \code{alpha_lambdas}.}
\item{theta_idx}{Index into \code{theta_lambdas}.}
\item{seed_idx}{Random seed used for that job.}
\item{fold_idx}{CV fold index (1..\code{nfold}).}
\item{lambda_alpha}{Actual \code{alpha_lambdas[alpha_idx]}.}
\item{lambda_theta}{Actual \code{theta_lambdas[theta_idx]}.}
}
}
\description{
Creates the full factorial grid over simulation IDs, CV folds, random seeds,
and the indices of \code{alpha_lambdas} and \code{theta_lambdas}. It also
attaches the actual \code{lambda_alpha} and \code{lambda_theta} values for
each row. Each row corresponds to one cross-validation job.
}
\examples{
\dontrun{
idx_mat <- simul_idx_matrix(
  n_sims        = 3,
  alpha_lambdas = c(1e-3, 1e-2),
  theta_lambdas = c(1e-3, 1e-2, 1e-1),
  seeds         = 1:2,
  nfold         = 5
)
head(idx_mat)
}

}